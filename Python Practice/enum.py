from enum import Enum
#This data type allouws you to create sets of named constants - considered members of the containing enum
#Essentially no one can reassign the variable 
INACTIVE = 0
ACTIVE = 1
    
print(list(State))